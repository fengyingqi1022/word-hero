# 单词小勇士：产品需求规格说明书

> 文档版本：1.0  
> 产品阶段：MVP（第一版）  
> 文档状态：已确认，可进入开发  
> 目标载体：单页静态 Web 应用（核心代码可集中在一个 `index.html` 中）

---

## 1. 产品概述

“单词小勇士”是一款供同一家庭中两名孩子使用的英语单词学习工具。两名孩子分别为五年级和初三，使用完全独立的词库、词包、学习进度和复习记录。

家长通过 CSV 文件导入单词。孩子通过看中文输入英文、听发音输入英文、看英文回忆中文以及跟读等方式进行主动回忆。系统使用基于间隔重复的复习计划安排旧词复习，并通过“当前词包全部掌握后解锁下一词包”的机制控制学习进度。

第一版不使用账号系统、服务器数据库或云端同步。所有业务数据保存在当前浏览器中，并支持导出和恢复完整备份。

---

## 2. 产品目标

### 2.1 核心目标

1. 让两名不同年级的孩子在同一个应用中使用各自独立的词库。
2. 通过主动输入和听写提高记忆效果，而不是只浏览单词卡。
3. 按照明确、可执行的间隔重复规则安排复习。
4. 以实际掌握程度而非学习天数决定何时解锁下一词包。
5. 让家长可以用简单的 CSV 文件维护词库。
6. 同时支持电脑浏览器和 iPad Safari。
7. 在不使用服务器数据库的条件下，提供可靠的本地保存、备份和恢复能力。

### 2.2 第一版不包含

- 用户注册、登录和密码找回
- 云端数据库和多设备实时同步
- 教师后台、班级管理和排行榜
- 自动语音识别或发音评分
- AI 生成例句、图片或助记内容
- 原生 iOS、Android 或桌面安装包
- 强制离线 PWA 能力
- 两个孩子之间的成绩比较
- 复杂的游戏、商城、积分兑换或社交功能

---

## 3. 目标用户与使用场景

### 3.1 用户

| 用户 | 年级 | 默认每轮新词 | 默认建议时长 |
|---|---|---:|---:|
| 孩子一 | 五年级 | 10 个 | 约 15 分钟 |
| 孩子二 | 初三 | 15 个 | 约 20 分钟 |
| 家长 | 管理者 | 不适用 | 不适用 |

姓名、头像颜色、年级和每轮新词数均可修改。

### 3.2 典型场景

1. 家长在电脑或 iPad 中准备多个 CSV 词包。
2. 家长选择孩子，再为该孩子导入一个或多个词包。
3. 孩子进入应用并选择自己的个人档案。
4. 系统优先安排到期复习，再安排当前词包的新词。
5. 孩子完成互动练习；系统记录正确、模糊或错误。
6. 当前词包内所有单词达到当前掌握后，下一个词包立即解锁。
7. 已通关词包中的旧词继续按照间隔计划复习。
8. 家长定期把完整数据备份到本地文件夹或 iCloud Drive。

---

## 4. 产品原则

1. **主动回忆优先**：输入题是主要题型，选择题只用于困难时的辅助。
2. **掌握驱动进度**：解锁词包不依赖日期或连续学习天数。
3. **通关不等于永久记住**：词包通关后，旧词仍继续复习。
4. **两个孩子完全隔离**：任何词库或进度操作都必须明确归属某个孩子。
5. **对孩子友好**：错误反馈应鼓励改正，不使用惩罚性语言。
6. **对家长简单**：第一版 CSV 只要求英文和中文两列。
7. **数据可带走**：本地数据必须能够完整导出和恢复。

---

## 5. 核心术语

| 术语 | 定义 |
|---|---|
| 词库 | 某个孩子拥有的全部单词集合，由多个词包组成。 |
| 词包 | 一次导入的一个 CSV 文件，可包含任意数量的单词。 |
| 当前词包 | 正在学习新词的词包。同一孩子同时只能有一个当前词包。 |
| 锁定词包 | 前序词包尚未通关，暂时不能开始学习的词包。 |
| 当前掌握 | 单词在当前学习阶段连续两次无提示答对。 |
| 词包通关 | 词包内全部单词均达到当前掌握。 |
| 长期掌握 | 单词完成 60 天阶段的间隔复习。 |
| 到期复习 | `nextReviewDate` 小于或等于今天的复习任务。 |
| 模糊 | 接近正确、使用提示后答对，或孩子自评“有点模糊”。 |

---

## 6. 平台与发布方式

### 6.1 支持平台

- iPad：当前主流版本的 Safari
- 电脑：当前主流版本的 Chrome、Edge 和 Safari
- 页面需要同时适配触摸和鼠标键盘操作

### 6.2 推荐发布方式

- 将应用作为静态 HTTPS 网页发布。
- iPad 用户可通过 Safari 打开，并添加到主屏幕。
- 电脑用户通过网址打开应用。
- 第一版可以把 HTML、CSS 和 JavaScript 集中在一个 `index.html` 中。
- CSV 文件不需要上传到服务器；浏览器在用户主动选择文件后于本地解析。

### 6.3 第一版限制

- 应用数据只属于当前浏览器和当前设备，不自动同步。
- 清理浏览器网站数据可能删除本地学习记录，因此必须提供备份功能。
- 第一版不承诺断网后首次打开应用仍可使用。

---

## 7. 信息架构与页面

### 7.1 选择孩子页

显示两个孩子的卡片：

- 姓名或昵称
- 年级
- 头像颜色
- 当前词包
- 今日待复习数量

点击卡片进入对应孩子的学习首页。不需要登录或密码。

### 7.2 学习首页

显示：

- 当前孩子的姓名和头像颜色
- 今日到期复习数量
- 当前词包名称及通关比例
- 当前词包已掌握数／总数
- “开始学习”或“继续学习”按钮
- 最近一次学习结果摘要
- 词包、报告和设置入口

学习页面和管理页面必须始终显示当前孩子，避免操作错对象。

### 7.3 单词学习页

包含：

- 题目区域
- 英文发音按钮
- 文本输入框或自评按钮
- 分级提示按钮
- 提交按钮
- 当前进度，例如“8 / 20”
- 提交后的答案对比和反馈
- “下一个”按钮

### 7.4 本轮结果页

显示：

- 本轮题目数
- 完全正确数
- 模糊数
- 错误数
- 新达到当前掌握的单词数
- 本轮需要再次练习的单词
- 当前词包进度
- “继续练习”“返回首页”按钮

词包通关时额外显示通关提示和“进入下一词包”按钮。

### 7.5 词包管理页

提供：

- 导入一个或多个 CSV
- 电脑端拖放导入
- 词包列表和单词数量
- 当前、已通关、已解锁、锁定状态
- 调整尚未开始词包的顺序
- 修改词包显示名称
- 查看导入错误和重复项
- 更新、归档或删除词包
- 删除尚未开始学习的误导入词包
- 由家长切换唯一的当前词包

### 7.6 学习报告页

显示：

- 当前词包和完成比例
- 已导入、已通关词包数量
- 已学习单词数
- 当前掌握单词数
- 长期掌握单词数
- 今日到期及已完成复习数
- 最近七天正确率和学习时长
- 错误次数最多的单词
- 容易遗忘的单词
- 连续学习天数

第一版不显示两个孩子之间的排名。

### 7.7 设置与备份页

提供：

- 修改姓名、年级和头像颜色
- 设置每轮新词数量
- 设置发音速度
- 开关自动播放发音
- 导出完整备份
- 恢复完整备份
- 重置某个孩子的学习进度
- 清空某个孩子的全部数据

重置和清空属于危险操作，必须二次确认，并明确说明影响范围。

---

## 8. 个人档案与数据隔离

1. 应用首次打开时创建两个个人档案槽位。
2. 两个档案分别默认设置为“五年级”和“初三”，但可以修改。
3. 每个档案拥有独立的：
   - 设置
   - 词包顺序
   - 单词数据
   - 学习进度
   - 复习计划
   - 学习统计
4. 导入前必须先选择孩子。
5. 导入确认窗口必须显示“正在给谁导入”。
6. 切换孩子时，如果存在未结束的学习轮次，应先保存进度再切换。
7. 一个孩子的导入、删除、重置和恢复操作不得改变另一个孩子的数据。

---

## 9. CSV 词包规范

### 9.1 文件格式

- 文件扩展名：`.csv`
- 推荐编码：UTF-8 或 UTF-8 with BOM
- 第一行必须为表头
- 必填列：`word`、`meaning`
- 列名比较时忽略大小写和前后空格
- 应正确支持被双引号包裹、内部含逗号的 CSV 字段

示例：

```csv
word,meaning
apple,苹果
important,重要的；重要性大的
environment,环境
```

### 9.2 字段规则

| 字段 | 必填 | 规则 |
|---|---|---|
| `word` | 是 | 英文单词或短语，去除首尾空格后不得为空。 |
| `meaning` | 是 | 中文释义，多个释义建议用中文分号分隔。 |

音标、例句、词性、单元、教材和图片均不是第一版字段。未来增加可选列时，现有两列 CSV 必须继续兼容。

### 9.3 多文件导入

- 支持一次选择一个或多个 CSV。
- 每个 CSV 默认生成一个词包。
- 默认按文件名中的数字和名称排序，例如 `01_第一组.csv` 在 `02_第二组.csv` 之前。
- 导入后家长可以手动调整尚未开始词包的顺序。
- 词包数量和每包单词数量不设业务上的固定值。
- 空文件、缺少必填列或没有有效单词的文件不得创建词包。

### 9.4 导入结果

导入完成后显示：

- 成功导入的文件数和单词数
- 跳过的空行数
- 跳过的重复单词数
- 合并释义的单词数
- 失败的行号及原因

部分行错误时，允许导入其他有效行，但必须向家长展示错误摘要。

### 9.5 重复词规则

在同一个孩子的词库中，以规范化后的英文作为单词唯一键：

- 英文和中文均相同：跳过重复记录。
- 英文相同、中文不同：合并中文释义，并去除重复释义。
- 相同英文在不同词包出现时，只保留一份学习进度，但可以记录其所属词包。
- 两个孩子之间永远不共享单词进度。

英文规范化包括：转为小写、去除首尾空格、将连续空格合并为一个。

### 9.6 更新词包

当导入与现有词包同名的文件时，必须询问：

- 更新现有词包
- 作为新词包导入
- 取消

更新现有词包时：

- 英文未变化的单词保留学习进度。
- 中文释义可以更新。
- 新英文加入词包。
- 被移除的英文先归档，不立即删除学习历史。
- 更新不得清除或重算任何已有 `progress.nextReviewDate`。
- 已通关词包不允许覆盖更新，应作为新词包导入或取消。

---

## 10. 词包状态与解锁规则

### 10.1 词包状态

| 状态 | 含义 |
|---|---|
| `locked` | 尚不可学习。 |
| `available` | 已解锁，但尚未成为当前词包。 |
| `active` | 当前正在学习新词。 |
| `completed` | 词包内全部单词曾达到当前掌握。 |
| `archived` | 已归档，不再提供新词，但历史数据仍保留。 |

### 10.2 初始状态

- 一个孩子第一次导入词包后，排序第一的词包成为 `active`。
- 后续词包保持 `locked`。
- 一个孩子同时最多只有一个 `active` 词包。
- 家长可以把未通关词包切换为 `active`；原 `active` 词包变为 `available`。
- 切换只改变词包状态，不改变任何单词进度或复习日期。
- 孩子的学习会话只能从唯一的 `active` 词包取得新词。

### 10.3 单词达到当前掌握

一个单词满足以下条件时标记为“当前掌握”：

1. 连续两次答题结果为“无提示完全正确”。
2. 两次正确之间，该单词先回到当前轮次队列末尾；有其他题目时应先完成其他题目。
3. 至少一次是主动输入英文的题型。
4. 使用过提示、查看答案或出现轻微拼写错误的答题，不计入连续正确次数。

如果词包只有少量单词，可以在完成当前轮次其余题目后再次出现，不设置强制分钟数，因此可以在同一天甚至同一轮学习中达到当前掌握。

### 10.4 词包通关与解锁

- 当词包内全部有效单词在同一时点均处于当前掌握状态时，词包变为 `completed`。
- 下一个词包立即从 `locked` 变为 `available`，并允许孩子立即开始。
- 如果孩子选择继续，下一个词包变为 `active`。
- 如果孩子选择休息，下次学习时从该词包开始。
- 解锁不依赖日期、学习天数或词包大小。
- 已通关词包中的单词继续产生到期复习任务。
- 已通关词包中的单词后来答错，不得重新锁住任何后续词包。

---

## 11. 学习轮次生成规则

### 11.1 任务优先级

每次点击“开始学习”时，按以下顺序生成任务：

1. 所有已学习词包中的到期复习词。
2. 当前词包中尚未学习的新词。
3. 本轮答错或模糊、需要再次出现的词。

到期复习按以下顺序排列：

1. 逾期天数更多的优先。
2. 历史错误次数更多的优先。
3. 最近学习时间更早的优先。

### 11.2 每轮数量

- 五年级默认引入最多 10 个新词。
- 初三默认引入最多 15 个新词。
- 家长可以为每个孩子分别修改，建议允许范围为 1～50。
- 默认值是学习引导，不是每天的硬性上限。
- 完成本轮后，孩子可以继续下一轮。
- 如果到期复习数量大于该孩子每轮新词数的两倍，默认先生成纯复习轮次，并提示“先清理复习任务，再学习新词”。孩子仍可在完成该轮后继续。

### 11.3 中断与恢复

- 每完成一道题立即保存结果。
- 关闭页面、刷新或切换孩子后，已提交的题目不得丢失。
- 未提交的输入内容无需恢复。
- 返回时可以选择继续未结束轮次或重新生成剩余任务。

---

## 12. 互动题型

### 12.1 新词认识卡

单词首次出现时展示：

- 英文
- 中文释义
- 播放发音按钮
- 跟读提示
- “开始练习”按钮

认识卡不计为一次正确答题。

### 12.2 看中文输入英文

- 显示中文释义。
- 孩子在输入框填写英文。
- 提交后展示孩子答案、正确答案和差异。
- 这是主要的主动回忆题型。

### 12.3 听发音输入英文

- 默认不显示英文。
- 提供播放和重复播放按钮。
- 孩子输入听到的英文。
- 提交后展示正确答案和中文释义。

### 12.4 看英文回忆中文

- 显示英文，可播放发音。
- 孩子先回忆中文；可以选择输入，也可以直接在脑中作答。
- 展示标准释义后，由孩子选择：
  - 答对了
  - 有点模糊
  - 不会
- 该题型可以帮助检查含义，但不能单独完成“至少一次输入英文”的掌握条件。

### 12.5 跟读

- 展示单词答案后，提供标准发音播放按钮。
- 提示孩子跟读。
- 可以记录“已跟读”，但不影响答题正确性和掌握判定。
- 第一版不录音、不识别语音、不进行发音评分。

### 12.6 题型分配

复习题默认比例：

- 50% 看中文输入英文
- 30% 听发音输入英文
- 20% 看英文回忆中文

约束：

- 单词达到当前掌握前，至少出现一次输入英文题。
- 连续两次测试同一单词时，条件允许应使用不同题型。
- 选择题只在单词连续答错时作为辅助提示，不作为完全掌握依据。

---

## 13. 提示系统

提示按以下顺序逐级提供：

1. 播放英文发音
2. 显示英文字符数量
3. 显示首字母
4. 显示部分字母
5. 显示完整答案

规则：

- 播放发音在听写题中属于题目本身，不算提示；在看中文输入英文题中算一级提示。
- 使用任何提示后，即使答案正确，也记为“模糊”。
- 查看完整答案后，本题记为“错误／查看答案”。
- 提示使用情况必须写入该次答题记录。

---

## 14. 英文答案判定

### 14.1 比较前规范化

对孩子答案和标准答案执行：

1. 去除首尾空格。
2. 转为小写。
3. 将连续空格合并为一个。
4. 忽略末尾的句号、问号和感叹号。

规范化后完全相同，且未使用提示，判为“完全正确”。

### 14.2 轻微拼写错误

满足以下条件时可显示为“接近正确／模糊”：

- 标准答案长度大于 3；并且
- 与标准答案的编辑距离为 1。

接近正确仍然不计入连续正确次数。页面应高亮多写、少写或写错的字符。

长度不超过 3 的单词，只要不完全匹配即判为错误，避免把关键字母错误误判为接近正确。

### 14.3 中文答案

第一版不对中文自由输入做严格自动判分，原因是近义词和表达方式较多。中文题以展示标准答案后的自我评价为准。

---

## 15. 复习算法

### 15.1 间隔阶段

单词首次达到当前掌握后，按照以下间隔安排复习：

| 阶段 | 下次复习间隔 |
|---:|---:|
| 0 | 1 天 |
| 1 | 2 天 |
| 2 | 4 天 |
| 3 | 7 天 |
| 4 | 15 天 |
| 5 | 30 天 |
| 6 | 60 天 |

通过阶段 6 的复习后，单词标记为“长期掌握”。长期掌握词不再进入强制阶段表，但可以进入低频抽查池；第一版建议每 90 天抽查一次。

### 15.2 日期定义

- 复习计划使用设备当地日期，不要求精确到某个小时。
- `nextReviewDate <= 今天` 即为到期。
- 逾期不降低等级、不扣分；下次打开应用时优先补做。

### 15.3 结果处理

#### 无提示完全正确

- 连续正确次数增加。
- 对已进入间隔复习的单词，通过当前阶段并进入下一阶段。
- 设置新的 `nextReviewDate`。

#### 模糊或使用提示

- 不通过当前复习阶段。
- 放回本轮队列，在若干其他题后再次出现。
- 当前复习阶段最多回退一级，最低为阶段 0。
- 本轮最终重新答对后，按照回退后阶段对应的间隔安排下一次复习。

#### 错误或查看答案

- 当前连续正确次数清零。
- 当前掌握状态设为否，但不改变已经通关的词包状态。
- 复习阶段重置为阶段 0。
- 放回本轮队列，优先在至少 3 道其他题后再次出现；若剩余题目不足，则放在轮次末尾。
- 需要重新连续两次无提示答对，才恢复当前掌握。

### 15.4 核心伪代码

```text
if answer == CORRECT_WITHOUT_HINT:
    consecutiveCorrect += 1

    if word is not currently mastered:
        if consecutiveCorrect >= 2 and hasEnglishInputSuccess:
            currentlyMastered = true
            reviewStage = 0
            nextReviewDate = today + 1 day
        else:
            enqueueAtEndOfSession(word)
    else if this is a scheduled review:
        if reviewStage < 6:
            reviewStage += 1
            nextReviewDate = today + intervalFor(reviewStage)
        else:
            longTermMastered = true
            nextReviewDate = today + 90 days

else if answer == FUZZY_OR_HINTED:
    consecutiveCorrect = 0
    reviewStage = max(reviewStage - 1, 0)
    enqueueLaterInSession(word)

else if answer == WRONG_OR_REVEALED:
    consecutiveCorrect = 0
    currentlyMastered = false
    reviewStage = 0
    longTermMastered = false
    enqueueLaterInSession(word)
```

实现时必须编写自动化测试覆盖所有阶段跳转和边界条件。

---

## 16. 发音功能

- 使用浏览器提供的英文文本转语音能力朗读 `word`。
- 默认优先使用英语语音。
- 家长可以设置慢速、正常速度；默认正常偏慢。
- 新词认识卡可自动播放一次，设置中允许关闭。
- 用户可以无限次手动重播。
- 如果设备没有可用英语语音或朗读失败，应显示友好提示，并允许继续学习。
- 发音失败不得阻塞任何非听写题。
- 听写题在没有可用发音时应自动替换为看中文输入英文题。

---

## 17. 学习报告与激励

### 17.1 指标定义

- 正确率 = 完全正确次数 ÷ 已提交题目次数。
- 当前掌握数 = `currentlyMastered = true` 的单词数。
- 长期掌握数 = `longTermMastered = true` 的单词数。
- 学习时长只统计页面处于前台且最近 2 分钟内有操作的时间。
- 连续学习天数按设备当地日期计算；某日完成至少一道题即视为学习过。

### 17.2 推荐激励

- 连续答对 10 个
- 完成一次学习轮次
- 一个词包通关
- 连续学习 3 天、7 天、30 天
- 一个单词达到长期掌握

激励只针对个人历史，不比较两个孩子。

---

## 18. 本地保存与数据模型

### 18.1 存储要求

- 每次提交答案、导入词包或修改设置后立即持久化。
- 优先使用 IndexedDB 保存完整应用数据；少量界面偏好可使用 `localStorage`。
- 所有存储数据必须包含 `schemaVersion`，以支持后续升级。
- 初始化或升级失败时不得覆盖现有数据，应提示用户先导出或重试。

### 18.2 建议数据结构

```text
AppData
├── schemaVersion
├── profiles[]
│   ├── id
│   ├── name
│   ├── grade
│   ├── avatarColor
│   ├── settings
│   ├── packOrder[]
│   └── statistics
├── packs[]
│   ├── id
│   ├── profileId
│   ├── name
│   ├── sourceFileName
│   ├── status
│   ├── order
│   ├── createdAt
│   ├── updatedAt
│   └── wordKeys[]
├── words[]
│   ├── profileId
│   ├── wordKey
│   ├── word
│   ├── meaning
│   └── packIds[]
├── progress[]
│   ├── profileId
│   ├── wordKey
│   ├── currentlyMastered
│   ├── longTermMastered
│   ├── consecutiveCorrect
│   ├── hasEnglishInputSuccess
│   ├── reviewStage
│   ├── firstSeenAt
│   ├── lastReviewedAt
│   ├── nextReviewDate
│   ├── correctCount
│   ├── fuzzyCount
│   └── wrongCount
└── attempts[]
    ├── profileId
    ├── wordKey
    ├── timestamp
    ├── questionType
    ├── result
    ├── usedHint
    └── responseDurationMs
```

`profileId + wordKey` 共同构成单词进度唯一键，确保两个孩子完全隔离。

---

## 19. 备份与恢复

### 19.1 导出备份

- 一键导出 JSON 格式完整备份。
- 备份包含两个孩子的个人资料、词库、词包顺序、学习进度、答题历史和设置。
- 文件名建议包含产品名和导出日期。
- 每隔 7 天未备份时，在家长管理区域显示非阻塞提醒。
- 文件可保存到电脑文件夹、iPad“文件”或 iCloud Drive。

### 19.2 恢复备份

恢复前必须：

1. 验证 JSON 格式和 `schemaVersion`。
2. 展示备份日期、两个孩子名称、词包数和单词数。
3. 让家长选择“覆盖当前全部数据”或“取消”。
4. 二次确认覆盖操作。
5. 恢复成功后重新加载首页，并显示恢复摘要。

无效或不兼容的备份不得部分覆盖现有数据。

### 19.3 跨设备使用

第一版不自动同步。需要在一台设备导出完整备份，再在另一台设备恢复。产品内应明确说明这一限制。

---

## 20. 错误处理

必须覆盖以下情况：

| 情况 | 处理方式 |
|---|---|
| CSV 不是 UTF-8 | 尝试读取；乱码或失败时提示另存为 UTF-8 CSV。 |
| 缺少 `word` 或 `meaning` | 阻止该文件导入并指出缺失列。 |
| 某行缺少英文或中文 | 跳过该行并显示行号。 |
| CSV 中存在逗号 | 支持标准双引号字段，不可简单按逗号切割。 |
| 重复单词 | 按重复词规则跳过或合并，并提供摘要。 |
| 发音不可用 | 替换听写题，不阻塞学习。 |
| 本地存储失败 | 停止继续写入，提示导出或释放存储空间，不静默丢失数据。 |
| 页面意外关闭 | 保留所有已提交答题结果。 |
| 设备日期改变 | 按当前本地日期重新计算到期任务，不产生负间隔。 |
| 备份文件损坏 | 不覆盖现有数据，显示失败原因。 |
| 删除已学习词包 | 二次确认，说明历史记录是否保留。 |

所有来自 CSV 的文本在页面显示时必须进行安全转义，不得作为 HTML 直接插入。

---

## 21. 非功能需求

### 21.1 易用性

- 主要按钮触摸区域不小于 44 × 44 CSS 像素。
- iPad 竖屏、横屏及电脑常见宽度下均可正常使用。
- 输入英文后可按 Enter 提交。
- 提交后焦点和软键盘行为应适合连续答题。
- 不使用仅靠颜色表达的状态；同时提供文字或图标。
- 儿童界面使用简短、积极、可理解的语言。

### 21.2 性能

- 1,000～10,000 个单词时，首页和学习页操作应保持流畅。
- 导入 1,000 个单词应显示明确的处理中状态，不能造成重复点击导入。
- 报告统计应使用聚合或分批计算，避免阻塞界面。

### 21.3 隐私

- 第一版不采集、上传或分析孩子的学习数据。
- 除打开网页和设备自身的语音能力外，核心学习流程不依赖第三方服务。
- 页面应明确说明数据保存在当前设备浏览器中。

### 21.4 可维护性

- 复习间隔、默认新词数、题型比例等应集中定义为配置常量。
- 数据迁移必须基于 `schemaVersion`。
- 核心算法应与页面渲染代码分离，便于自动化测试。

---

## 22. 验收标准

### 22.1 个人档案

- [ ] 首次打开能看到两个孩子的档案。
- [ ] 修改一个孩子的姓名和设置不会影响另一个孩子。
- [ ] 任意页面都能明确识别当前孩子。

### 22.2 CSV 与词包

- [ ] 能在电脑选择或拖放一个及多个 CSV。
- [ ] 能在 iPad Safari 从“文件”选择一个及多个 CSV。
- [ ] 仅含 `word`、`meaning` 两列的 CSV 可成功导入。
- [ ] 含双引号和字段内逗号的合法 CSV 可正确解析。
- [ ] 缺少必填字段时给出明确错误。
- [ ] 重复英文按规则跳过或合并。
- [ ] 多词包可以排序，并且只有当前词包引入新词。

### 22.3 学习与掌握

- [ ] 新词先显示认识卡，再进入测试。
- [ ] 支持看中文输入英文、听音输入英文和看英文回忆中文。
- [ ] 大小写和首尾空格不影响英文正确判定。
- [ ] 一个字母的拼写错误不会被判为完全正确。
- [ ] 使用提示后答对不会增加无提示连续正确次数。
- [ ] 连续两次无提示正确且至少一次输入英文后，单词成为当前掌握。
- [ ] 当前词包全部单词达到当前掌握后立即解锁下一词包。
- [ ] 解锁不依赖日期或学习天数。
- [ ] 旧词后来答错不会重新锁住后续词包。

### 22.4 复习算法

- [ ] 首次掌握后安排 1 天复习。
- [ ] 无提示答对后依次进入 2、4、7、15、30、60 天阶段。
- [ ] 模糊结果不通过当前阶段，并在本轮再次出现。
- [ ] 答错后阶段重置，并需要重新达到当前掌握。
- [ ] 逾期词在下次进入时优先出现，但不产生惩罚。
- [ ] 完成 60 天阶段后标记为长期掌握。

### 22.5 保存、备份与恢复

- [ ] 每次提交后刷新页面，已提交结果仍存在。
- [ ] 关闭再打开后可以继续学习。
- [ ] 能导出包含两个孩子完整数据的 JSON 备份。
- [ ] 有效备份可以在另一设备恢复。
- [ ] 损坏备份不会覆盖当前数据。
- [ ] 清除或覆盖数据前有二次确认。

### 22.6 兼容性与体验

- [ ] iPad Safari 中触摸、软键盘、发音和文件导入可正常工作。
- [ ] Chrome、Edge 和 Safari 桌面版中可正常使用。
- [ ] 发音不可用时仍能完成非听写学习。
- [ ] 五年级和初三档案分别应用默认每轮新词数。

---

## 23. 建议测试用例

1. 导入一个只有 3 个单词的词包，在同一轮内全部连续正确两次，验证能够立即通关。
2. 导入一个 100 个单词的词包，分多天学习，验证未全部掌握前不会解锁下一包。
3. 通关第一词包后故意答错其中一个旧词，验证第二词包不会被重新锁定。
4. 分别给两个孩子导入包含相同英文的 CSV，验证进度不共享。
5. 同一孩子的两个词包包含相同英文和不同中文释义，验证释义合并且进度唯一。
6. 输入大小写不同、前后空格、一个字母错误和完全错误的答案，验证四种结果。
7. 完成一次阶段复习后修改设备日期，验证系统不会生成非法负间隔。
8. 在学习中刷新页面，验证已提交结果保留、未提交文本可以丢弃。
9. 导出完整备份，清空数据，再恢复备份，验证两个孩子的词库和进度一致。
10. 模拟发音接口失败，验证听写题被替换且其他题型可继续。

---

## 24. 开发优先级

### P0：必须完成

- 两个孩子的独立档案
- CSV 单文件和多文件导入
- 词包排序、状态和掌握解锁
- 三种核心题型
- 英文答案判定和提示
- 间隔复习算法
- 本地持久化
- 完整备份与恢复
- iPad 和电脑响应式适配

### P1：第一版建议完成

- 学习报告
- 发音速度设置
- 个人成就提示
- 拖放导入
- 更新现有词包
- 七天备份提醒

### P2：后续版本

- 自动云同步
- 多家长或教师管理
- 自动语音识别和发音评分
- 音标、例句、词性和图片
- 更丰富的游戏化内容
- 原生应用或完整离线 PWA

---

## 25. 完成定义

当且仅当以下条件全部满足时，MVP 才算完成：

1. 所有 P0 功能实现。
2. 第 22 节全部验收项通过。
3. 第 23 节核心测试用例通过。
4. 在至少一台 iPad 和一台电脑上完成真实文件导入、学习、关闭重开和备份恢复测试。
5. 不存在会导致一个孩子的数据写入另一个孩子档案的问题。
6. 不存在刷新或关闭页面后丢失已提交学习记录的问题。
7. 家长不阅读技术说明，也能完成选择孩子、导入词包、开始学习和导出备份四个核心操作。
